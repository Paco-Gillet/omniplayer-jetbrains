# Omni Player

A media player directly integrated in your JetBrains IDE interface that use Windows media keys to control your music.

## Features

*   Control your music with native media keys (Play/Pause, Next, Previous).
*   Integrates as a Tool Window in the IntelliJ UI.

Still in development, stay tuned to see the progress.
